diff --git a/src/index/index_node_thread_pool_wrapper.cc b/src/index/index_node_thread_pool_wrapper.cc
index a0a003d0..8006e838 100644
--- a/src/index/index_node_thread_pool_wrapper.cc
+++ b/src/index/index_node_thread_pool_wrapper.cc
@@ -15,0 +16 @@
+#include "filemanager/AsyncInputStream.h"
@@ -41 +42,6 @@ IndexNodeThreadPoolWrapper::Search(const DataSetPtr dataset, std::unique_ptr<Con
-    return thread_pool_->push([&]() { return this->index_node_->Search(dataset, std::move(cfg), bitset, op_context); })
+    const auto s3_read_path_config =
+        op_context == nullptr ? milvus::S3ReadPathConfig{} : op_context->s3_read_path_config;
+    return thread_pool_->push([&, s3_read_path_config]() {
+            milvus::ScopedS3ReadPathConfig s3_read_path_scope(s3_read_path_config);
+            return this->index_node_->Search(dataset, std::move(cfg), bitset, op_context);
+        })
@@ -47,0 +54,2 @@ IndexNodeThreadPoolWrapper::RangeSearch(const DataSetPtr dataset, std::unique_pt
+    const auto s3_read_path_config =
+        op_context == nullptr ? milvus::S3ReadPathConfig{} : op_context->s3_read_path_config;
@@ -49 +57,4 @@ IndexNodeThreadPoolWrapper::RangeSearch(const DataSetPtr dataset, std::unique_pt
-        ->push([&]() { return this->index_node_->RangeSearch(dataset, std::move(cfg), bitset, op_context); })
+        ->push([&, s3_read_path_config]() {
+            milvus::ScopedS3ReadPathConfig s3_read_path_scope(s3_read_path_config);
+            return this->index_node_->RangeSearch(dataset, std::move(cfg), bitset, op_context);
+        })
